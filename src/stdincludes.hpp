#pragma once
#ifndef _STDINCLUDES_HPP
#define _STDINCLUDES_HPP

#include <algorithm>
#include <atomic>
#include <chrono>
#include <fstream>
#include <iomanip>
#include <iostream>
#include <random>
#include <string>
#include <thread>
#include <vector>

// yes i understand why this is not something to do on a large project
// yes i have decided to do it anyway
// this is not a large project and i may cut it back later anyway, but it is nice to have
// it has saved me some headache and retyping so it makes it easy for the moment
// this project takes < 2 seconds to compile at the moment anyway and maybe a Mb of memory so it is okay

#endif